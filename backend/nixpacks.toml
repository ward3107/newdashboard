[phases.setup]
nixPkgs = ["python312", "gcc", "gfortran", "pkg-config"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install numpy scipy --only-binary :all:",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
