# ============================================================================
# ISHEBOT OPTIMIZATION API - ENVIRONMENT CONFIGURATION
# ============================================================================

# FastAPI Configuration
APP_NAME=ISHEBOT Optimization API
APP_VERSION=1.0.0

# Development: DEBUG=True, Production: DEBUG=False
DEBUG=True

# Server Configuration
# Railway will override PORT with $PORT environment variable
HOST=0.0.0.0
PORT=8000

# ============================================================================
# CORS Configuration
# ============================================================================
# CRITICAL: Only allow your frontend domain(s) to access the API
#
# DEVELOPMENT: Include your local dev server (Vite runs on 5173 by default)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173
#
# PRODUCTION: Replace with your actual Vercel deployment URL
# Example:
# ALLOWED_ORIGINS=https://yourdomain.vercel.app,https://www.yourdomain.com
#
# SECURITY NOTE:
# - Never use wildcard (*) in production
# - Remove localhost URLs in production
# - Only add domains you own and trust

# ============================================================================
# Genetic Algorithm Settings
# ============================================================================
GA_POPULATION_SIZE=100
GA_GENERATIONS=100
GA_MUTATION_RATE=0.1
GA_CROSSOVER_RATE=0.8

# ============================================================================
# API Security
# ============================================================================
# CRITICAL: Generate a strong secret key for production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or: openssl rand -hex 32
SECRET_KEY=REPLACE_WITH_GENERATED_SECRET_KEY
ALGORITHM=HS256

# Rate Limiting
# Limits API requests per IP address to prevent abuse
# RATE_LIMIT_REQUESTS_PER_MINUTE: Sustained rate (tokens refilled per minute)
# RATE_LIMIT_BURST: Maximum burst size (requests allowed instantly)
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_BURST=10

# Logging
# Development: DEBUG or INFO, Production: INFO or WARNING
LOG_LEVEL=INFO

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
#
# For Railway Deployment:
# 1. Set DEBUG=False
# 2. Generate secure SECRET_KEY (openssl rand -hex 32)
# 3. Update ALLOWED_ORIGINS with your Vercel URLs
# 4. Set LOG_LEVEL=INFO
# 5. Railway will automatically set PORT via $PORT variable
#
# For Local Development:
# 1. Copy this file to .env
# 2. Keep DEBUG=True
# 3. Use localhost origins
# 4. Run with: uvicorn app.main:app --reload
#
