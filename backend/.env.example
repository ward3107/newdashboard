# ============================================================================
# ISHEBOT OPTIMIZATION API - ENVIRONMENT CONFIGURATION
# ============================================================================

# FastAPI Configuration
APP_NAME=ISHEBOT Optimization API
APP_VERSION=1.0.0

# Development: DEBUG=True, Production: DEBUG=False
DEBUG=True

# Server Configuration
# Cloud platforms (Render, Vercel, etc.) will override PORT with $PORT environment variable
HOST=0.0.0.0
PORT=8000

# ============================================================================
# CORS Configuration
# ============================================================================
# Development: Include all local development ports
# Production: Use your deployed Vercel frontend URL
#
# DEVELOPMENT:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3003,http://127.0.0.1:3000,http://127.0.0.1:3001,http://127.0.0.1:3003
#
# PRODUCTION:
# ALLOWED_ORIGINS=https://ishebot-dashboard.vercel.app,https://ishebot.vercel.app
#
# NOTE: Add all your deployment URLs (production + preview branches)

# ============================================================================
# Genetic Algorithm Settings
# ============================================================================
GA_POPULATION_SIZE=100
GA_GENERATIONS=100
GA_MUTATION_RATE=0.1
GA_CROSSOVER_RATE=0.8

# ============================================================================
# API Security
# ============================================================================
# IMPORTANT: Generate a strong secret key for production!
# You can generate one with: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256

# Logging
# Development: DEBUG or INFO, Production: INFO or WARNING
LOG_LEVEL=INFO

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
#
# For Production Deployment (Render, Vercel, etc.):
# 1. Set DEBUG=False
# 2. Generate secure SECRET_KEY (openssl rand -hex 32)
# 3. Update ALLOWED_ORIGINS with your frontend URLs
# 4. Set LOG_LEVEL=INFO
# 5. Platform will automatically set PORT via $PORT variable
#
# For Local Development:
# 1. Copy this file to .env
# 2. Keep DEBUG=True
# 3. Use localhost origins
# 4. Run with: uvicorn app.main:app --reload
#
